{
  "name": "void-note",
  "version": "1.0.0",
  "description": "Void note",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "esbuild renderer.js --bundle --outfile=renderer.bundle.js"
  },
  "author": "Gabryel-W",
  "license": "MIT",
  "devDependencies": {
    "electron": "23.1.3",
    "esbuild": "^0.25.1"
  },
  "dependencies": {
    "konva": "^9.3.20"
  }
}
